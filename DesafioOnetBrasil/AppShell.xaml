<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="DesafioOnetBrasil.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:DesafioOnetBrasil.View"
    Shell.FlyoutBehavior="Disabled"
    Title="DesafioOnetBrasil"
    Shell.TitleColor="{ StaticResource SecondaryDarkText }"
    Shell.BackgroundColor="{StaticResource PrimaryDark}">

    <ShellContent Title="Lista de Tarefas"
                  ContentTemplate="{ DataTemplate local:ListarTarefaPage }"
                  Route="ListarTarefa" />

</Shell>
